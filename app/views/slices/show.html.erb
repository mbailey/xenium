<table>
    <thead>
	<caption><%= @slice.name %> <%= link_to image_tag('/images/Edit.png', :alt => "Edit Slice", :title => "Edit Slice"), edit_slice_config_file_path(@slice.name) %></caption>
	</thead>
	<tbody>
	<tr><th class="name">status</th><td><span id="slice_controls_<%= @slice.name %>">
	<%= slice_control_buttons(@slice) %></span></td></tr>
	<tr><th class="name">memory</th><td><%= @slice.config_file.memory %></td></tr>
	<tr><th class="name">autostart?</th><td><%= @slice.config_file.auto ? 'on' : 'off' %></td></tr>
	<tr>
		<th class="name">Backups (<%= link_to 'create new backup', create_backup_slice_path(@slice.name), :method => :post %>)</th>
		<td>
			<ul id="backup_list">
			<% @slice.backups.each do |backup| %>
			<li><%= backup.version %></li>
			<% end %>
			</ul>
		</td>
	</tr>
		
	<%# @slice.config_file.vifs.each_with_index do |vif, index| %>
		<!--tr><th colspan="2" class="name">eth<%#= index %></th></tr>
		<tr><th class="name">name</th><td><%#= vif.vifname %></td>
		<tr><th class="name">bridge</th><td><%#= vif.bridge %></td>
		<tr><th class="name">ip</th><td><%#= vif.ip %></td>
		<tr><th class="name">mac</th><td><%#= vif.mac %></td-->
	<%# end %>

    </tbody>
	   
</table>

