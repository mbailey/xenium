<table>
	<caption><%= @domain.name %> <%= link_to 'edit', edit_domain_config_path(@domain.name) %> <%= domain_control_buttons(@domain) %></caption>
	<tr><th class="name">memory</th><td><%= @domain.config.memory %></td><tr>
	<tr><th class="name">autostart?</th><td><%= @domain.config.auto ? 'on' : 'off' %></td><tr>
		
	<%# @domain.config.vifs.each_with_index do |vif, index| %>
		<!--tr><th colspan="2" class="name">eth<%#= index %></th></tr>
		<tr><th class="name">name</th><td><%#= vif.vifname %></td>
		<tr><th class="name">bridge</th><td><%#= vif.bridge %></td>
		<tr><th class="name">ip</th><td><%#= vif.ip %></td>
		<tr><th class="name">mac</th><td><%#= vif.mac %></td-->
	<%# end %>
	</tr>
	<td>    
</table>
<%= link_to 'Edit', edit_domain_path(@domain) %> |
<%= link_to 'Back', domains_path %>
