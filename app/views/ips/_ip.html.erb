  <tr class="<%= cycle('odd','even') %>" >
	<td class="name"><%= link_to h(ip.address), ip %></td>
    <td><%=link_to h(ip.network.name), ip.network unless ip.network.blank? %></td>
    <td>
      <%#=h ip.interface.slice.name unless ip.interface.blank? %> 
	  <%#=h ip.interface.name unless ip.interface.blank? %>
	</td>
	<td></td>
	<td class="actions"><%= link_to image_tag('/images/Edit.png', :alt => "Edit", :title => "Edit"), edit_ip_path(ip) %></td>
	<td class="actions"><%= link_to image_tag('/images/destroy.png', :alt => "Destroy", :title => "Destroy"), ip, :confirm => "Are you sure you want to delete IP #{ip.address}?", :method => :delete %></td>
  </tr>